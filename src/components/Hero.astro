---
/**
 * Hero.astro — Homepage hero. Consolidation hook.
 * This is the opening statement. Sets tone for the entire page.
 *
 * Content is provisional — final copy TBD during beta.
 * Structure: tagline → headline → subtext → CTA → contextual angle links
 *
 * Narrative intent: "Stop juggling. One app does it all."
 * Layered with calm, mindful tone — not hype.
 */

import { APP_URL } from '@/data/pages';
---

<section class="w-full min-h-[70vh] flex flex-col items-center justify-center px-6 py-24 bg-gradient-to-b from-white to-stone-50">
  <div class="max-w-3xl mx-auto text-center space-y-6">

    <!-- Eyebrow / Tagline -->
    <p class="text-sm font-medium text-emerald-700 uppercase tracking-widest">
      {/* COPY PLACEHOLDER: Brief positioning line. Consolidation + calm. */}
      Personal finance. All in one place.
    </p>

    <!-- Main Headline — consolidation hook -->
    <h1 class="text-3xl md:text-5xl lg:text-6xl font-semibold text-stone-800 leading-tight tracking-tight">
      {/* COPY PLACEHOLDER: The core hook. One app replaces the juggling act. */}
      Budget. Track. Invest.<br />
      Finally, one place for all of it.
    </h1>

    <!-- Subtext — expand the promise -->
    <p class="text-base md:text-xl text-stone-500 leading-relaxed max-w-2xl mx-auto">
      {/* COPY PLACEHOLDER: Expand. Why consolidation matters. Privacy + intentionality layered in. */}
      Finzen combines envelope budgeting, spending tracking, and portfolio management
      into one beautifully simple app. Privacy-first. No bank connections. Just a clear,
      complete picture of your finances.
    </p>

    <!-- Primary CTA -->
    <div class="pt-4">
      <a
        href={APP_URL}
        class="inline-flex items-center gap-2 px-7 py-3.5 bg-emerald-700 text-white text-base font-medium rounded-xl hover:bg-emerald-800 transition-colors shadow-sm"
      >
        Get Started Free
        <svg width="18" height="18" viewBox="0 0 16 16" fill="none" aria-hidden="true">
          <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </a>
    </div>

    <!-- Contextual angle links (curated — not all) -->
    <div class="pt-6 flex flex-wrap items-center justify-center gap-x-6 gap-y-2">
      <a href="/privacy-first-finance-app" class="text-sm text-stone-400 hover:text-emerald-700 transition-colors">
        Privacy first →
      </a>
      <a href="/visual-financial-insights" class="text-sm text-stone-400 hover:text-emerald-700 transition-colors">
        See your money →
      </a>
      <a href="/manual-personal-finance-app" class="text-sm text-stone-400 hover:text-emerald-700 transition-colors">
        Why manual →
      </a>
    </div>
  </div>
</section>
