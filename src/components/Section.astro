---
/**
 * Section.astro — Generic content section wrapper.
 * Handles consistent padding, max-width, and optional heading.
 *
 * Usage:
 *   <Section heading="Why Manual Tracking Works" subheading="Optional subtitle">
 *     <p>Content here...</p>
 *   </Section>
 *
 *   <Section heading="Features" align="center">
 *     ...
 *   </Section>
 */

interface Props {
  /** Section heading (renders as <h2>) */
  heading?: string;
  /** Optional subheading below the heading */
  subheading?: string;
  /** Text alignment */
  align?: 'left' | 'center';
  /** Background variant */
  bg?: 'white' | 'stone' | 'dark';
  /** Additional CSS classes on the outer wrapper */
  class?: string;
}

const {
  heading,
  subheading,
  align = 'left',
  bg = 'white',
  class: extraClass = '',
} = Astro.props;

const bgClasses = {
  white: 'bg-white',
  stone: 'bg-stone-50',
  dark: 'bg-stone-900 text-white',
};

const alignClasses = {
  left: 'text-left',
  center: 'text-center',
};
---

<section class={`w-full py-16 md:py-24 ${bgClasses[bg]} ${extraClass}`}>
  <div class={`max-w-4xl mx-auto px-6 ${alignClasses[align]}`}>

    {heading && (
      <div class="mb-10 space-y-3">
        <h2 class={`text-2xl md:text-3xl font-semibold text-stone-800 ${bg === 'dark' ? 'text-white' : ''} leading-tight`}>
          {heading}
        </h2>
        {subheading && (
          <p class={`text-base md:text-lg ${bg === 'dark' ? 'text-stone-300' : 'text-stone-500'} leading-relaxed max-w-2xl ${align === 'center' ? 'mx-auto' : ''}`}>
            {subheading}
          </p>
        )}
      </div>
    )}

    <slot />
  </div>
</section>
