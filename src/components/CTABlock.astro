---
/**
 * CTABlock.astro — Reusable call-to-action section.
 * Used at the bottom of homepage and angle pages.
 * Accepts custom heading and optional subtext.
 *
 * Usage:
 *   <CTABlock
 *     heading="Start your daily practice"
 *     subtext="No credit card. No bank connection. Just clarity."
 *   />
 */

import { APP_URL } from '@/data/pages';

interface Props {
  heading: string;
  subtext?: string;
  buttonLabel?: string;
}

const {
  heading,
  subtext,
  buttonLabel = 'Get Started Free',
} = Astro.props;
---

<section class="w-full py-20 md:py-28 px-6 bg-gradient-to-b from-stone-50 to-emerald-50">
  <div class="max-w-2xl mx-auto text-center space-y-5">

    <h2 class="text-2xl md:text-4xl font-semibold text-stone-800 leading-tight">
      {heading}
    </h2>

    {subtext && (
      <p class="text-base md:text-lg text-stone-500 leading-relaxed">
        {subtext}
      </p>
    )}

    <div class="pt-2">
      <a
        href={APP_URL}
        class="inline-flex items-center gap-2 px-7 py-3.5 bg-emerald-700 text-white text-base font-medium rounded-xl hover:bg-emerald-800 transition-colors shadow-sm"
      >
        {buttonLabel}
        <svg width="18" height="18" viewBox="0 0 16 16" fill="none" aria-hidden="true">
          <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </a>
    </div>
  </div>
</section>
